# Edition JSON files are generated by scripts/dump_editions_to_json.py
# from the editions / articles tables. They are committed (so CF Pages
# can build without DB access), but this .gitkeep ensures the directory
# is tracked even before any dump runs.
