---
// Scaffold placeholder. Replaced by the real archive index (#42) once
// the build-time data flow (Python → web/src/content/editions/*.json
// → Astro Content Collection) is wired up.
const title = "日々 (Hibi) — scaffold";
---
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
  </head>
  <body>
    <main>
      <h1>{title}</h1>
      <p>
        Astro is installed. The real layout pulls
        <code>design-system/colors_and_type.css</code> via
        <code>&lt;link&gt;</code> once #41 lands.
      </p>
    </main>
  </body>
</html>
